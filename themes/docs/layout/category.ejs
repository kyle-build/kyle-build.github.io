<article class="doc">
  <header class="doc-hero">
    <h1><%= page.category %></h1>
  </header>
  <div class="prose">
    <ul>
      <% page.posts.forEach(function(post) { %>
        <li><a href="<%= url_for(post.path) %>"><%= post.title %></a></li>
      <% }) %>
    </ul>
  </div>
</article>
